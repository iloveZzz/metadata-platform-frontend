/**
 * Generated by orval v6.31.0 🍺
 * Do not edit manually.
 * 数据质量只读观测（DQ Insight）API
 * 独立 API 包（/api/dq/*），覆盖外部 DQ 结果接入（GE / 通用 CSV / 通用 API）、健康分查询（资产级 / 字段级）、 规则明细钻取、仪表盘聚合、通道管理（CRUD + 启停 + 重试）、待关联资产处理（人工映射）与审计日志查询。 响应统一使用 YSS Result / PageResult 包装；错误体支持 field path / code / message / severity（参照 metadata-platform 冻结约定）。 freeze-candidate：契约草案，尚未 Freeze（批准由主控 Agent 出具）；Freeze 前不得作为前后端实现契约。

 * OpenAPI spec version: 0.1.0-frozen
 */
import type { RuleResultRow } from './ruleResultRow';
import type { SourceTool } from './sourceTool';

/**
 * 外部 DQ 结果接入请求体（application/json，GE 结果 / 通用 API 结果）
 */
export interface DQResultSubmit {
  /** 主平台资产 ID（可选；顶层声明时对该批次整体挂接） */
  assetId?: string;
  /** 批次号；幂等去重键之一（与 sourceTool 组合，SB-09 已确认 MVP 基线：服务端按批次号去重，重复批次 409；完整认证与幂等契约 P1） */
  batchNo: string;
  /** 关联接入通道 ID（可选；走 API 推送通道时关联） */
  channelId?: string;
  /** 数据域（可选；未关联前的初步分域展示） */
  domain?: string;
  /** 工具执行时间 = 结果时间（有效期起算，OQ-03） */
  executionTime: string;
  results: RuleResultRow[];
  sourceTool: SourceTool;
}
