/**
 * Generated by orval v6.31.0 🍺
 * Do not edit manually.
 * 数据质量只读观测（DQ Insight）API
 * 独立 API 包（/api/dq/*），覆盖外部 DQ 结果接入（GE / 通用 CSV / 通用 API）、健康分查询（资产级 / 字段级）、 规则明细钻取、仪表盘聚合、通道管理（CRUD + 启停 + 重试）、待关联资产处理（人工映射）与审计日志查询。 响应统一使用 YSS Result / PageResult 包装；错误体支持 field path / code / message / severity（参照 metadata-platform 冻结约定）。 freeze-candidate：契约草案，尚未 Freeze（批准由主控 Agent 出具）；Freeze 前不得作为前后端实现契约。

 * OpenAPI spec version: 0.1.0-frozen
 */
import type { DashboardStatsBandDistribution } from './dashboardStatsBandDistribution';

/**
 * 仪表盘聚合统计（基于数据域内可见资产全集）
 */
export interface DashboardStats {
  /** 健康分分布（优 / 良 / 差 + 过期 / 无结果独立展示态；无结果与过期不归入档位） */
  bandDistribution?: DashboardStatsBandDistribution;
  /** 覆盖率 % = 已接入（含过期）÷ 数据域内可见目标资产 × 100（口径见 SB-07 已确认） */
  coverage?: number;
  /** 已接入资产数（有 DQ 结果，含过期） */
  ingestedAssetCount?: number;
  /** 低分资产数（档位 = 差） */
  lowScoreAssetCount?: number;
  /** 数据域内可见目标资产数（防腐层拉取主平台口径） */
  targetAssetCount?: number;
}
